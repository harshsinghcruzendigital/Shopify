{%- comment -%}
  loading-spinner.liquid — reusable spinner. Param: class (optional).
{%- endcomment -%}
<span class="mg-spinner {{ class }}" role="status" aria-label="{{ 'general.accessibility.loading' | t | default: 'Loading' }}"></span>
